
##############################
### WINDOWS AND WORKSPACES ###
##############################

# See https://wiki.hypr.land/Configuring/Window-Rules/ for more
# See https://wiki.hypr.land/Configuring/Workspace-Rules/ for workspace rules

# Example windowrules that are useful

windowrule {
    # Ignore maximize requests from all apps. You'll probably like this.
    name = suppress-maximize-events
    match:class = .*

    suppress_event = maximize
}

windowrule {
    # Fix some dragging issues with XWayland
    name = fix-xwayland-drags
    match:class = ^$
    match:title = ^$
    match:xwayland = true
    match:float = true
    match:fullscreen = false
    match:pin = false

    no_focus = true
}

# Hyprland-run windowrule
windowrule {
    name = move-hyprland-run

    match:class = hyprland-run

    move = 20 monitor_h-120
    float = yes
}

# #spotify pop over window rule
# windowrule {
#     name = spotify-scratchpad
#     match:class = spotify
#     float = on
#     center = on
#     size = 1400 900
#     workspace = special:spotify silent
# }
 
windowrule = workspace special:music, match:class Spotify
windowrule = workspace special:music, match:initial_title Spotify( Free)?  # Spotify on Wayland has no class
windowrule = workspace special:todo, match:class superproductivity
windowrule = workspace special:communication, match:class discord